import React from 'react'

export default ({area}) => {
    return (
        <div>
            this is the hike page
            <div>
                {area.name}
            </div>
        </div>
    )
}