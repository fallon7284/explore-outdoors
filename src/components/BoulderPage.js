import React from 'react'

export default ({area}) => {
    return (
        <div>
            this is the boulder page
            <div>
                {area.name}
            </div>
        </div>
    )
}