import React from 'react'

export default ({area}) => {
    return (
        <div>
            this is the camp page
            <div>
                {area.name}
            </div>
        </div>
    )
}